<Page x:Class="PddTrainingApp.Views.ImportPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Импорт вопросов">

    <Grid Background="#f5f5f5">
        <ScrollViewer>
            <StackPanel Margin="20">
                <Button Content="← Назад" Click="BackButton_Click"
                        Background="Transparent" Foreground="#007ACC" 
                        Padding="10,5" FontSize="14" HorizontalAlignment="Left"
                        BorderThickness="0" Cursor="Hand" Margin="0,0,0,10"/>

                <TextBlock Text="Импорт вопросов" FontSize="24" FontWeight="Bold" 
                           Margin="0,0,0,20" Foreground="#333"/>

                <Border Background="White" Padding="20" CornerRadius="10">
                    <StackPanel>
                        <TextBlock Text="Выберите JSON файл для импорта:" 
                                   FontWeight="Bold" Margin="0,0,0,10"/>

                        <Button Content="📁 Выбрать файл" Click="SelectFile_Click"
                                Background="#f39c12" Foreground="White" 
                                Padding="15,10" FontWeight="Bold" Cursor="Hand"
                                HorizontalAlignment="Left"/>

                        <TextBlock x:Name="SelectedFileText" Margin="0,10,0,0"
                                   Foreground="#666" TextWrapping="Wrap"/>

                        <Button Content="Импортировать" Click="ImportFile_Click"
                                Background="#27ae60" Foreground="White" 
                                Padding="15,10" FontWeight="Bold" Cursor="Hand"
                                Margin="0,20,0,0" HorizontalAlignment="Left"/>

                        <TextBlock x:Name="ImportResultText" Margin="0,10,0,0"
                                   TextWrapping="Wrap" FontWeight="SemiBold"/>
                    </StackPanel>
                </Border>

                <Border Background="White" Padding="20" CornerRadius="10" Margin="0,20,0,0">
                    <StackPanel>
                        <TextBlock Text="Инструкция по импорту:" 
                                   FontWeight="Bold" Margin="0,0,0,10"/>

                        <TextBlock TextWrapping="Wrap" Foreground="#666">
                            • Поддерживается формат JSON, экспортированный из этой системы
<LineBreak/>• Файл должен содержать текст вопроса и варианты ответов
<LineBreak/>• Если модуль не существует, он будет создан автоматически
<LineBreak/>• После импорта вопрос появится в соответствующем модуле
                        </TextBlock>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>