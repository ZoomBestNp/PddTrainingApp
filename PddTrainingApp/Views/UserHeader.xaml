<UserControl x:Class="PddTrainingApp.Views.UserHeader"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Background="#2c3e50" Padding="15,10">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="ПДД Тренер" Foreground="White" FontSize="20" FontWeight="Bold"
                       VerticalAlignment="Center"/>

            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <Border x:Name="StudentCodeBorder" Background="#e67e22" Padding="8,4" CornerRadius="4" 
                        Margin="0,0,10,0" Visibility="Collapsed">
                    <TextBlock x:Name="StudentCodeText" Foreground="White" FontSize="11" FontWeight="Bold"/>
                </Border>

                <Button x:Name="ProfileButton" Click="ProfileButton_Click"
                        Background="Transparent" BorderThickness="0" 
                        Padding="10,5" Cursor="Hand">
                    <StackPanel Orientation="Horizontal">
                        <Border Width="32" Height="32" Background="#3498db" CornerRadius="16">
                            <TextBlock x:Name="UserInitialsText" Text="?" Foreground="White" 
                                       FontWeight="Bold" FontSize="14"
                                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>

                        <TextBlock x:Name="UserNameText" Text="Гость" Foreground="White" 
                                   FontSize="14" Margin="10,0,5,0"
                                   VerticalAlignment="Center"/>

                        <TextBlock Text="▼" Foreground="White" FontSize="10"
                                   VerticalAlignment="Center" Margin="0,2,0,0"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>